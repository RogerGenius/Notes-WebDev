# 下载安装

**概述：** Consul 是 HashiCorp 开发的服务发现与配置管理工具，使用 Go 编写，安装包为单个可执行文件，无需额外依赖。==Consul 版本需与 Spring Cloud 版本兼容==。

```
下载地址：https://developer.hashicorp.com/consul/install
```

**概述：** 下载对应操作系统的压缩包，解压后得到一个 `consul`（或 `consul.exe`）可执行文件。建议将其放在固定目录并添加到系统 `PATH` 环境变量，以便在任意位置使用 `consul` 命令。

## Linux 安装

```bash
# Debian/Ubuntu
wget -O- https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install consul

# CentOS/RHEL
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/RHEL/hashicorp.repo
sudo yum -y install consul
```

## Windows / macOS

**概述：** Windows 下载 `.zip` 包解压即可；macOS 推荐使用 Homebrew：

```bash
# macOS
brew install consul
```

## 验证安装

```bash
consul version
# 输出示例：Consul v1.17.0
```

# 启动 Consul

## 开发模式

**概述：** 开发环境使用 `-dev` 模式启动，该模式以==单节点==方式运行，数据存储在内存中（重启后丢失），适合本地开发和测试。

```bash
consul agent -dev
```

**概述：** 默认端口为 ==8500==，启动成功后访问 `http://localhost:8500` 可看到 Consul 的 Web UI 管理界面。界面中 "Services" 页面展示所有已注册的服务实例及其健康状态。

```mermaid
flowchart LR
    A[下载解压 consul] --> B[consul agent -dev]
    B --> C["访问 localhost:8500"]
    C --> D[Consul Web UI]
```

## 生产模式（集群）

**概述：** 生产环境必须以集群模式运行。最低配置为 ==3 个 Server 节点==（容忍 1 个节点故障），推荐 5 个（容忍 2 个）。Server 数量必须为奇数，以满足 Raft 协议的多数派要求。

```bash
# Server 节点 1（引导启动）
consul agent -server -bootstrap-expect=3 \
  -data-dir=/opt/consul/data \
  -node=server-1 -bind=10.0.1.1 \
  -client=0.0.0.0 -ui

# Server 节点 2、3（加入集群）
consul agent -server -bootstrap-expect=3 \
  -data-dir=/opt/consul/data \
  -node=server-2 -bind=10.0.1.2 \
  -join=10.0.1.1

# Client 节点（运行在微服务所在机器）
consul agent -data-dir=/opt/consul/data \
  -node=client-1 -bind=10.0.2.1 \
  -join=10.0.1.1
```

**概述：** 集群关键参数：

| 参数 | 说明 |
|------|------|
| `-server` | 以 Server 模式运行 |
| `-bootstrap-expect=N` | 集群预期 Server 数，达到后自动选举 Leader |
| `-data-dir` | 数据持久化目录（==生产必须配置==） |
| `-bind` | 集群内通信地址 |
| `-client` | 客户端接口监听地址，`0.0.0.0` 允许远程访问 |
| `-join` | 加入已有集群的节点地址 |
| `-ui` | 启用 Web UI |

# 常用管理命令

```bash
# 查看集群成员
consul members

# 查看集群 Leader
consul operator raft list-peers

# 查看已注册服务
consul catalog services

# KV 操作
consul kv put key value
consul kv get key
consul kv delete key

# 优雅退出集群
consul leave
```

## Further Reading

- [Consul 官方安装文档](https://developer.hashicorp.com/consul/install)
- [Consul Agent 命令参考](https://developer.hashicorp.com/consul/commands/agent)
- [Consul 集群部署指南](https://developer.hashicorp.com/consul/tutorials/production-deploy)
- [Consul Web UI 文档](https://developer.hashicorp.com/consul/docs/connect/observability/ui)
